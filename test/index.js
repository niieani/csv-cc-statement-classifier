import test from "tape"
import moneyProject from "../"

test("moneyProject", (t) => {
  t.plan(1)
  //t.equal(true, moneyProject(), "return true")
  //return true;
  t.equal(true, true, "return true");
})
